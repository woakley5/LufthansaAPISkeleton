project 'LufthansaAPITest.xcodeproj'

# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'LufthansaAPITest' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for LufthansaAPITest
  pod 'Alamofire'
  pod 'SwiftyJSON'
  pod 'ObjectMapper'
  
  target 'LufthansaAPITestTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'LufthansaAPITestUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
